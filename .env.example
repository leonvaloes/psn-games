# Chave da API RAWG — obtenha em https://rawg.io/apidocs
RAWG_API_KEY=sua_chave_aqui

# Segredo para assinar JWT — gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=troque_por_um_segredo_forte_de_pelo_menos_32_chars
